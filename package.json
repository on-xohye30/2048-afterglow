{
  "name": "2048_plusplus",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "2048_plusplus: an original Korean 2048 game with daily puzzles and private friend leagues",
  "scripts": {
    "test": "node --test engine.test.mjs league-game.test.mjs ui-model.test.mjs backend/backend.test.mjs",
    "start": "node server.mjs",
    "build": "node build.mjs",
    "dev:league": "npm run build && npx wrangler dev --config wrangler.local.jsonc",
    "deploy:league": "npm run build && npx wrangler deploy --config wrangler.local.jsonc"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "devDependencies": {
    "wrangler": "4.136.3"
  }
}
