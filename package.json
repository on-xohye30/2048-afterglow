{
  "name": "2048-afterglow",
  "version": "2.0.1",
  "private": true,
  "type": "module",
  "description": "An original, dependency-free 2048 playground with daily and 5x5 modes",
  "scripts": {
    "test": "node --test engine.test.mjs league-game.test.mjs backend/backend.test.mjs",
    "start": "node server.mjs",
    "build": "node build.mjs",
    "dev:league": "npm run build && npx wrangler dev --config wrangler.local.jsonc",
    "deploy:league": "npm run build && npx wrangler deploy --config wrangler.local.jsonc"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "devDependencies": {
    "wrangler": "4.136.3"
  }
}
